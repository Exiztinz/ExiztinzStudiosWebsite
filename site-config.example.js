window.SITE_CONFIG = {
  sanity: {
    // Safe to expose on frontend for published content
    projectId: 'yourProjectId',
    dataset: 'production',
    apiVersion: '2024-10-01',
    useCdn: true,
    postsLimit: 3,
    // Optional: link to a full news archive
    moreNewsUrl: '#'
  }
};
